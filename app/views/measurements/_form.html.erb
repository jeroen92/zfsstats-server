<%= simple_form_for(@measurement) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :value %>
    <%= f.input :quantity %>
    <%= f.input :name %>
    <%= f.input :device %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
